Fixtures are **generated, never committed as photographs or scans**. Run
`make fixtures` (or `python tests/fixtures/make_fixtures.py`) to build them.

Nothing here may derive from a photograph of a person. That is the whole reason the
test suite exists as procedural geometry.
