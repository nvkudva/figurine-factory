Bambu Studio preset JSONs go here — they are machine config, not secrets, so they are
tracked. Export from Bambu Studio: preset dropdown -> "..." -> Export preset.

Required: `p1s_machine.json`, `p1s_0.16_standard.json`, `pla_basic.json`.
Names must match `configs/printers/bambu_p1s_0.4.yaml`.
